[build]
  command = "bun run build"
  publish = "dist"

[build.environment]
  BUN_VERSION = "1.2.15"

# SPA fallback: every unknown path serves index.html so client-side routing works
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
